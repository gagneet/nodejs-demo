// 'chalk' package defines the colour of the console to write in. This was in node.js format
//var chalk = require("chalk");

// Using the Babel syntax for the above statement
import chalk from 'chalk';

// on the console, write a pre-start message in green colour.
console.log(chalk.green("Starting the App in the DEV Environment..."));
